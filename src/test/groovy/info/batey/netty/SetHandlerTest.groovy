package info.batey.netty

import spock.lang.Specification

/**
 * Created by chbatey on 04/01/2015.
 */
class SetHandlerTest extends Specification {
//    def "Should store in the memcache storage"() {
//        given:
//        def memcacheStorage = Mock(MemcacheStorage)
//        def underTest = new SetHandler(memcacheStorage)
//        def memcacheMessage = new MemcacheMessage(Command.SET, "batey", 0, 0, 100);
//
//        when:
//        underTest.channelRead(null, MemcacheMessage)
//    }
}
